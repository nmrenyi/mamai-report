\section{mamabench: QA Benchmark Construction}

\subsection{Schema and Canonical Format}
% JSONL row schema (v0.3 for benchmark v0.1; v0.4 for benchmark v0.2);
% set_types (mcq, open_ended, open_ended_rubric); core fields
% (id, schema_version, set_type, question, choices, answer,
% answer_index, source).

\subsection{Source Adapters}
% Per-source normalization to the canonical schema:
%   - MedMCQA (filtered OBGYN/Pediatrics subset)
%   - MedQA-USMLE (filtered OBGYN scope)
%   - AfriMed-QA MCQ and SAQ
%   - Kenya Clinical Vignettes
%   - WHB stumps
%   - HealthBench (oss_eval, consensus, hard subsets)
% Per-source manifests record source URL, license, prepared-input
% provenance, and validation report.

\subsection{AfriMed-QA Cleanup}
% Two hard filters: multi-answer rows (unrepresentable by single
% answer_index); ambiguous-answer-position rows (unscorable as MCQ).
% One row-level normalization: letter-prefix stripping when >= 2
% choices begin with their own letter label.
% Accepted cosmetic issues (single-choice prefixes, soft prefixes,
% short stem questions, duplicate non-answer options, non-ASCII
% characters) -- documented but not patched.

\subsection{OBGYN Scope Classifier}
% Five-category schema (MATERNAL, NEONATAL, CHILD_HEALTH,
% SEXUAL_AND_REPRODUCTIVE_HEALTH, NONE). Prompt v8, modular assembly
% (6 markdown modules) via load_classifier_prompt; openended and mcq
% modes. Qwen3.6-27B-FP8 with thinking-on at temperature 0; vLLM
% guided_json structured output; native reasoning captured in
% *_reasoning.jsonl audit side-files. Cross-job sharding and in-process
% concurrency.

\subsection{Judge-Calibration Side-File}
% v0.2.1 addition: calibration/obgyn_meta_eval.jsonl -- 6,853
% physician-labeled (prompt, completion, criterion) triples across 872
% prompts, derived from HealthBench's meta-eval set filtered to
% mamabench scope (CHILD_HEALTH 3,239, MATERNAL 2,284, SRH 942,
% NEONATAL 388). Purpose: lets any consumer validate an LLM judge
% against physician ground truth before trusting rubric scores.

\subsection{Release Manifest and Distribution}
% Per-source manifests aggregated into a release manifest with summed
% counts_by_set_type and counts_by_source_dataset, validated for
% benchmark_version / schema_version consistency; error_count = 0 on
% all sources. HuggingFace dataset nmrenyi/mamabench with one git tag
% per release (v0.1, v0.2, v0.2.1; v0.2.1 is an additive patch over
% v0.2 adding the calibration side-file). License propagation
% (e.g., AfriMed-QA CC-BY-NC-SA 4.0).
% Final size: 25,949 rows -- mcq 23,241 (MedMCQA 18,508, MedQA-USMLE
% 4,199, AfriMed-QA 534); open_ended 369 (Kenya 312, AfriMed-SAQ 37,
% WHB 20); open_ended_rubric 2,339 (oss_eval 1,209, consensus 872,
% hard 258); plus a 12,211-row HealthBench criteria side-table.
