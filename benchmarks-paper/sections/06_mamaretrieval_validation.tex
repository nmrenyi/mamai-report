\section{mamaretrieval Validation}

\subsection{Invariant Verification}
% Automated checks on every record (rubric-arithmetic and dimension-
% implication constraints). Phase 2b: 78,571 records, 0 violations,
% 0 errors. Tier 3: 230,964 records, 0 errors.

\subsection{Score Distribution}
% Phase 2b (0--2 rubric): 0 = 38,762 (49.3%); 1 = 15,023 (19.1%);
% 2 = 24,786 (31.6%). Tier 3 graded distribution from
% AUDIT_REPORT_v2.md / results_v2_tier3.json.

\subsection{Judge Calibration}
% Tier 1 pilot: production judge (Qwen3.5-397B) vs Claude Opus 4.7
% reference labels on 62 (q, c) pairs -- 95% threshold agreement at
% score >= 3 (lenient), 85% at >= 5 (strict). These two thresholds are
% the lenient/strict relevance cuts used in all scoreboards.

\subsection{Manual Spot Check}
% 10-sample stratified review (3 each at scores 0/1/2 plus one seed-
% chunk reference case). All judgments defensible; nuanced cases
% (drug-name collisions across clinical contexts; section-header
% mis-reads) handled correctly.

\subsection{Pool Completeness}
% The original plan was a 30-query exhaustive audit; it was superseded
% by something strictly larger -- tell that story:
% (1) The v1 audit measured the Phase 2a pool (top-10 from 3 retrievers)
%     against a 6-retriever pool on 100 queries: lenient recall ~0.49,
%     i.e. the initial pooling missed about half the relevant material.
% (2) That finding motivated Tier 3: top-20 from all 6 retrievers on all
%     3,185 queries (230,964 pairs) -- the released label set's pool.
% (3) Residual incompleteness beyond the 6-retriever top-20 union is
%     the remaining caveat; voyage HR@20 = 1.000 (lenient) suggests the
%     pool is near-saturated at the top of the ranking.
% This subsection is what Paper 1 cites for label provenance.

\subsection{Metric Sensitivity Under Incomplete Labels}
% Robust under pooling: Hit Rate@k, MRR.
% Moderately sensitive: nDCG@k.
% Most sensitive: Recall@k, Precision@k.
% Lead headline tables with robust metrics; report the rest as
% secondary with the pool-completeness caveat.
