\section{End-to-End Evaluation on mamabench}

% Full system: deployed retriever + generator. Compared against alternatives
% across model size and presence/absence of RAG.

\subsection{Model x RAG Matrix}
% Models: Gemma 4 E4B (deployed), MedGemma 4B, Meditron-70B, Llama-3
% (or Qwen 2.5-72B), GPT-5 (or Claude / Gemini). Conditions: no-RAG vs
% +RAG over the deployment guideline corpus.
% Methodological constraint: identical RAG pipeline (retriever, embedder,
% chunker, top-k, prompt template) across all "+RAG" rows; only the
% generator changes.

\subsection{MCQ Accuracy and Calibration}
% Accuracy, Brier score, ECE per cell of the matrix.

\subsection{Open-Ended Rubric Scores}
% HealthBench-style: factuality, reasoning, harm, omission, guideline
% adherence.

\subsection{Safety}
% EquityMedQA / FairMedQA pass rates; MedEqualQA / MedFuzz robustness
% deltas; clinical-safety judge scores; any safety-1 occurrences flagged.

\subsection{Corpus-Composition Ablation}
% Gemma + RAG over: WHO-only, Tanzania-only, Zanzibar-only, full corpus.
% Compared against Gemma no-RAG. Identifies which sources drive utility.

\subsection{Six Headline Comparisons}
% (1) Gemma no-RAG -> Gemma +RAG: value added by retrieval.
% (2) Gemma +RAG vs MedGemma no-RAG: fine-tuning vs RAG at same scale.
% (3) Gemma +RAG vs Frontier no-RAG: deployability headline.
% (4) Frontier no-RAG vs Frontier +RAG: does the corpus add information
%     beyond pretraining for a frontier model?
% (5) Gemma +RAG vs Mid-size/Frontier +RAG: model-size headroom at
%     equal knowledge.
% (6) Gemma no-RAG vs Mid-size/Frontier no-RAG: raw capability gap.
