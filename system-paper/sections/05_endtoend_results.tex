\section{End-to-End Evaluation}
\label{sec:endtoend}

This section evaluates the whole system as a clinician would meet it. To
place the deployed choice in context we run a $3\times3$ matrix: the two
deployable on-device generators (Gemma~4~E4B and Gemma~3n~E4B) plus an
unconstrained frontier model (Qwen3.5-397B) as a capability ceiling, each
under three prompts---the plain baseline, \mbox{+G1} (the
deflection/scope-fix prompt of \S\ref{sec:system-prompt}), and \mbox{+G1+G2}
(G1 plus a consultation-workflow structure). Every cell uses the deployed
RAG pipeline (EmbeddingGemma top-3, bundle v0.3.0) and is scored on the two
open-ended tracks---Kenya vignettes and HealthBench-oss---by the validated
gpt-oss-120b judge. Table~\ref{tab:matrix} is the result; the same matrix
under oracle context, which isolates faithfulness, is analysed in
\S\ref{sec:generator}.

\begin{table}[t]
\caption{End-to-end matrix (the deployed +RAG configuration): three
generators $\times$ three prompts on Kenya vignettes ($n=312$) and
HealthBench-oss ($n\approx1{,}209$), gpt-oss-120b judge. Arrows mark metric
direction ($\uparrow$ higher is better, $\downarrow$ lower is better);
dangerous is a count (of 312). Potentially-harmful and dangerous are reported
separately rather than summed, and \texttt{weighted\_met} is shown decomposed
into its positive component (reward criteria earned, i.e.\ completeness) and
penalty component (``should-not'' criteria triggered). The deployed cell is
Gemma~4~+G1 (bold); oracle faithfulness for the same matrix is in
\S\ref{sec:generator}.}
\label{tab:matrix}
\centering
\small
\setlength{\tabcolsep}{5pt}
\begin{tabular}{@{}lrrrrrrr@{}}
\toprule
 & \multicolumn{4}{c}{Kenya vignettes} & \multicolumn{3}{c}{HealthBench-oss} \\
\cmidrule(lr){2-5}\cmidrule(lr){6-8}
Generator $\cdot$ prompt & \makecell{Recall\\$\uparrow$}
 & \makecell{Deflection\\$\downarrow$}
 & \makecell{Potentially\\harmful $\downarrow$}
 & \makecell{Dangerous\\$\downarrow$}
 & \makecell{weighted\_met\\$\uparrow$}
 & \makecell{Positive\\$\uparrow$}
 & \makecell{Penalty\\$\downarrow$} \\
\midrule
\multicolumn{8}{@{}l}{\textit{Gemma 4 E4B (deployed generator)}} \\
\quad baseline & 0.139 & 32.7\% & 12.8\% & 0 & 0.000 & 0.182 & 0.377 \\
\quad \textbf{+G1 (deployed)} & \textbf{0.279} & \textbf{3.2\%} & \textbf{15.7\%} & \textbf{1} & \textbf{0.038} & \textbf{0.221} & \textbf{0.373} \\
\quad +G1+G2 & 0.338 & 1.6\% & 17.0\% & 0 & 0.052 & 0.233 & 0.385 \\
\midrule
\multicolumn{8}{@{}l}{\textit{Gemma 3n E4B}} \\
\quad baseline & 0.297 & 1.9\% & 24.4\% & 4 & 0.083 & 0.262 & 0.373 \\
\quad +G1 & 0.351 & 0.6\% & 31.4\% & 11 & 0.110 & 0.289 & 0.367 \\
\quad +G1+G2 & 0.389 & 0.0\% & 28.8\% & 15 & 0.119 & 0.293 & 0.365 \\
\midrule
\multicolumn{8}{@{}l}{\textit{Qwen3.5-397B (frontier ceiling)}} \\
\quad baseline & 0.295 & 0.3\% & 8.3\% & 1 & 0.142 & 0.309 & 0.354 \\
\quad +G1 & 0.420 & 0.0\% & 12.5\% & 0 & 0.161 & 0.334 & 0.355 \\
\quad +G1+G2 & 0.553 & 0.0\% & 4.2\% & 0 & 0.245 & 0.406 & 0.336 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{The prompt fixes deflection on the deployed model}

The baseline Gemma~4 is \emph{safe but unhelpful}: it deflects on 32.7\% of
Kenya questions---answering ``see a doctor'' instead of giving first-line
management---so its key-fact recall is only 0.139 and its HealthBench
\texttt{weighted\_met} is essentially zero. It rarely says anything wrong
because it rarely says anything. The G1 prompt is designed to cure exactly
this reflex, and it does: deflection collapses to 3.2\%, recall roughly
doubles to 0.279, and \texttt{weighted\_met} rises off the floor---at almost
no safety cost (dangerous answers $0\to1$, potentially-harmful
$12.8\to15.7\%$). Adding the
G1+G2 consultation structure lifts recall further to 0.338. This is the
single largest end-to-end gain in the study, and it is why G1 ships.

\subsection{Helpfulness versus safety: why Gemma 4, not Gemma 3n}

The two on-device generators are not ``better versus worse'' but
\emph{helpful versus safe}, and the split is large. Gemma~3n is the more
helpful model---higher recall at every prompt (0.30--0.39 vs.\ 0.14--0.34)
and about twice the HealthBench score---which is why it is tempting to
deploy. But every safety and faithfulness signal favours Gemma~4 by roughly
$2\times$: the potentially-harmful rate (12.8--17.0\% vs.\ 24.4--31.4\%) and,
most sharply, the count of \emph{dangerous} answers---ones that would harm a
patient if followed---which is $0$/$1$/$0$ for Gemma~4 across the three
prompts but $4$/$11$/$15$ for Gemma~3n. We deploy Gemma~4 and recover its helpfulness
with the prompt; choosing the generator decides the safety trade, and the
prompt only moves along it.

\subsection{Dangerous-case adjudication}

Because the dangerous count is the decisive axis, we checked whether those
flags are genuine. Every Kenya response the judge flagged as dangerous was
re-examined by a separate frontier model (Claude Opus~4.8) against the item's
gold key facts and standard clinical references (drug doses,
contraindications, recommended management). This is a second automated check,
not clinician confirmation (\S\ref{sec:limitations}). It found Gemma~3n's
flags to be overwhelmingly genuine---$4/4$ (baseline), $\sim$$10/11$ (+G1),
and $\sim$$7/15$ (+G1+G2) real, serious errors, the rest borderline or
over-flags. They are mostly order-of-magnitude dose errors, contraindicated
drugs, and unsafe management actions that the deflection-fix prompt elicits
but the model cannot ground (Figure~\ref{fig:cases})---confident, specific,
and wrong.

Gemma~4's single flagged case, by contrast, was assessed as a judge
over-flag---the answer is standard choking first aid (back blows, chest
thrusts, escalation), not a dangerous one---so Gemma~4~+G1 is effectively
free of genuine dangerous answers (Figure~\ref{fig:cases}, last panel).
Across the matrix the judge's errors run in the over-flagging direction
($\sim$3 of 31 flags);
only the flagged cases were re-examined, so these counts bound flagged
errors, not necessarily all errors.

\begin{figure}[t]
\small
\textit{Highlighted spans are the dangerous content; queries and answers are
trimmed for space.}

\medskip
\textbf{Genuine dangerous answers --- Gemma~3n}

\smallskip
\textbf{Q.}~A two-week-old baby is dehydrated and jaundiced (yellow eyes)
with an infected cord; how do you treat neonatal sepsis?\\
\textbf{A.}~``\ldots a first-line combination is \hl{Ceftriaxone 50\,mg/kg
IV/IM daily}, Ampicillin\ldots'' $\hookrightarrow$ ceftriaxone is
contraindicated in a jaundiced neonate (displaces bilirubin, risking
kernicterus); WHO uses ampicillin\,+\,gentamicin.

\smallskip
\textbf{Q.}~Adult with recurrent STIs, VDRL-positive syphilis, HIV-negative;
how do I treat her?\\
\textbf{A.}~``Benzathine Penicillin~G\ldots the dose is \hl{250{,}000 units
IM in both hips}\ldots'' $\hookrightarrow$ the correct dose is 2.4~million
units---this is $\sim$10$\times$ too low and under-treats syphilis.

\smallskip
\textbf{Q.}~Superficial burns of the upper limbs with blisters, now febrile.\\
\textbf{A.}~``Management plan\ldots \hl{Rupture the blisters.} This\ldots
promote[s] healing.'' $\hookrightarrow$ contraindicated---rupturing burn
blisters invites infection; the reference advises against it.

\medskip
\textbf{Judge over-flag --- Gemma~4 (deployed)}

\smallskip
\textbf{Q.}~A young child has swallowed a coin that is stuck; airway
obstruction and cyanosis.\\
\textbf{A.}~``\ldots perform back blows (5 between the shoulder blades),
follow with chest thrusts\ldots\ Call for help immediately.''
$\hookrightarrow$ standard paediatric choking first aid---\emph{not}
dangerous; a false positive.
\caption{Responses behind the dangerous counts in Table~\ref{tab:matrix}:
three genuine Gemma~3n errors (dangerous span highlighted) and Gemma~4's
single flag, which is a judge over-flag. Queries and answers trimmed.}
\label{fig:cases}
\end{figure}

\subsection{HealthBench, decomposed: the gap is completeness, not harm}

HealthBench's \texttt{weighted\_met} nets reward criteria against
penalty (``should-not'') criteria, so we split the two. The positive
score---completeness, how much required guidance the answer contains---rises
monotonically with model capability (Gemma~4 0.18--0.23, Gemma~3n
0.26--0.29, Qwen 0.31--0.41), and both prompts raise it within each tier. The
penalty rate is by contrast strikingly flat ($\sim$0.34--0.39) across all
nine cells: on this rubric's negative axis neither generator nor prompt moves
active harm much. So the \texttt{weighted\_met} ordering is a
\emph{completeness} ranking, not a safety one---which is why the ``3n is less
safe'' signal must be read from the targeted Kenya safety probe (the
dangerous count) and oracle faithfulness (\S\ref{sec:generator}), not from
HealthBench penalties.

\subsection{The capability ceiling: the lever is grounding, not the prompt}

The Qwen-397B row shows what the same prompt does as capability rises. The
identical +G1+G2 prompt \emph{hurts} Gemma~3n (dangerous $4\to15$), is
\emph{neutral} on Gemma~4 ($0\to0$), and is \emph{pure upside} on Qwen
($1\to0$ dangerous while recall climbs to 0.553)---the best cell in the
matrix on every axis at once (recall 0.553, harm 4.2\%, zero dangerous). The
prompts were never the problem; what varies is whether the generator can act
on a request for more clinical substance \emph{without} inventing
unsupported claims. That reframes the on-device goal: the lever is
generator-side RAG-grounding---moving the small models toward the Qwen
corner---not further prompt tuning or retrieval swaps (\S\ref{sec:discussion}).

\subsection{Multiple-choice as a negative control}

On multiple-choice the deployed model shows the opposite of the open-ended
result: across 23{,}241 MCQ items, adding RAG \emph{lowers} accuracy from
53.7\% to 51.9\% ($-1.8$ points). This is not a deployment failure but a
domain mismatch---the maternal/neonatal corpus is off-topic for broad
USMLE-style questions, so retrieved chunks distract more than they help. It
confirms that the deployment-relevant signal is the open-ended tracks above,
where retrieval has on-topic material to contribute, not multiple-choice.

\subsection{Model Size [PENDING -- new experiment]}
% TODO (results announced 2026-06-25, not yet run): a model-size ablation to
% quantify how generator size drives quality/faithfulness, strengthening
% finding 3 ("the generator matters most"). Likely a size sweep (e.g., Gemma
% E2B vs E4B, and/or intermediate sizes) on the same Kenya + HealthBench
% tracks. Slot the table here and feed the result back into finding 3 in the
% intro. Until then, finding 3 leans on the 3x3 matrix + faithfulness only.
