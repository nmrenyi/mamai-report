\section{Introduction}

% Scope of this section:
% - Clinical context: nurses and midwives in Zanzibar; OBGYN, neonatal, and
%   reproductive-health caseloads; gap between guideline knowledge and
%   point-of-care availability.
% - The on-device constraint: no reliable internet, low-end Android devices,
%   intermittent power. Why a server-based RAG system is not deployable here.
% - Safety priority: incorrect medical advice has direct patient-harm
%   consequences. Evaluation quality is the top engineering concern, not
%   performance or features.
% - Honest framing: a feature-complete, pre-deployment prototype with a
%   comprehensive evaluation -- not yet a fielded system (no user study
%   this cycle).
% - Mention the live web demo + open-source repos (footnote / end of intro).
% - Contributions, framed top-down to match the body order:
%   1. An on-device medical RAG system (Gemma 4 E4B + EmbeddingGemma-300M +
%      SQLite, G1 prompt) deployable on commodity Android hardware.
%   2. A bottom-up evaluation (end-to-end, retrieval, generator) presented
%      top-down, with a designed prompt (G1) that cuts deflection 32.7% ->
%      3.2% and ~doubles key-fact recall.
%   3. Findings that localize the failure mode, against intuition:
%      on-device retrieval is largely SOLVED (EmbeddingGemma 3rd of 7,
%      within ~8 pp of cloud); yet better retrieval does NOT reach the
%      answers (RAG net-neutral-to-negative on the small generator); the
%      deciding axis is generator faithfulness, where Gemma 4 (2.6-3.6%
%      true-hallucination) matches a frontier model and ~halves Gemma 3n;
%      so the deployed model is chosen by a safety-vs-usefulness tradeoff,
%      and generator-side RAG-grounding is the open problem.
%   4. On-device latency characterization, including a root-caused FP16-GPU
%      context-length failure at ~5,000 tokens.
% - Roadmap: section pointers.
