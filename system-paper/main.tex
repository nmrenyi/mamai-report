%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% MAM-AI -- System and Evaluation -- arXiv tech report
% ACM journal style (acmart, acmsmall, nonacm)
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Force arXiv (and any latexmk default) to process this with pdfLaTeX in PDF
% mode -- acmart's fonts work under pdflatex, the engine arXiv uses for it.
\pdfoutput=1

\documentclass[acmsmall,nonacm]{acmart}

% Drop ACM-specific frames that do not apply to arXiv tech reports.
\settopmatter{printacmref=false, printccs=false, printfolios=true}
\renewcommand\footnotetextcopyrightpermission[1]{}
\pagestyle{plain}

\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{microtype}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, fit, backgrounds}
% Figure palette (teal accent + warm amber), used in the pipeline figure and
% the judge-calibration chart so the paper reads consistently.
\definecolor{mamteal}{HTML}{1F7A8C}
\definecolor{mamamber}{HTML}{B5670E}
\usepackage{pgfplots}  % line charts (model-size ladder)
\pgfplotsset{compat=1.18}
\usepgfplotslibrary{groupplots}
\usepackage{float}     % [H] to pin the case-study figure in place
\usepackage{listings}
\usepackage{placeins}  % \FloatBarrier to keep tables within their section
\usepackage{makecell}  % multi-line table headers
\usepackage{soul}      % \hl highlighting for dangerous spans in examples
\colorlet{dangbg}{red!16}
\sethlcolor{dangbg}
\lstdefinestyle{prompt}{
  basicstyle=\ttfamily\footnotesize,
  breaklines=true,
  breakindent=0pt,
  columns=fullflexible,
  keepspaces=true,
  frame=single,
  framesep=6pt,
  xleftmargin=4pt,
  xrightmargin=4pt,
  aboveskip=1em,
  belowskip=1em,
}

% The author's name in Chinese is a small vector graphic (figures/renyi-cjk.pdf,
% the glyphs set in FandolSong, image box = the font's TeX box). This keeps the
% paper on pdflatex (the engine arXiv uses for acmart) -- no Unicode engine. The
% size (0.933em = the glyphs' height+depth) and baseline drop (0.16em = their
% depth) are in em, so \zhname scales to the surrounding font like a real glyph,
% matching both the large title and the small running head. \DeclareRobustCommand
% protects the graphicx keys from acmart's author uppercasing (which would mangle
% height= into HEIGHT=), and hyperref strips \includegraphics from PDF-string
% sinks (pdfauthor / bookmarks / hyperxmp XMP), so no CJK reaches them.
\DeclareRobustCommand{\zhname}{%
  \raisebox{-0.16em}{\includegraphics[height=0.933em]{figures/renyi-cjk}}}

\bibliographystyle{ACM-Reference-Format}
\citestyle{acmauthoryear}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Front matter
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\title{MAM-AI: An On-Device Medical Retrieval-Augmented Generation System
for Nurses and Midwives in Zanzibar}

\author{Ren Yi\,\zhname}
\orcid{0009-0002-6911-2855}
\affiliation{%
  \institution{École Polytechnique Fédérale de Lausanne}
  \country{Switzerland \quad bonjour@renyi.ch \\ orcid.org/0009-0002-6911-2855}
}
% Show the email on the author line (above) rather than in the journal
% "Author's Contact Information" footnote, which we suppress here.
\authorsaddresses{}

\begin{document}

\begin{abstract}
Maternal and newborn mortality remain among the highest in sub-Saharan Africa,
where midwifery care is often delivered by nurses who lack midwifery training to
international standards, and consulting authoritative guidance at the point of
care is hard---the guidelines are long and connectivity is intermittent. We
present MAM-AI, a medical question-answering assistant for nurse-midwives in
Zanzibar that runs entirely on a commodity Android device: a question is embedded
(EmbeddingGemma, 300M) and matched against a curated corpus of 87 guideline
documents (63{,}650 passages), then answered with citations by a 4B int4
generator (Gemma~4~E4B)---fully offline, with no query leaving the device. We evaluate the exact deployed
configuration with a layered, bottom-up methodology---retriever, generator under
oracle context, end-to-end, and latency---scored by LLM judges validated against
physician-written rubrics. The evaluation relocates the hard problem. On-device retrieval is essentially solved: the 300M
embedder ranks third of seven retrievers and rivals cloud systems, so the
passages the system needs are usually found. The small generator is what remains
in doubt: adding retrieved context does not improve its answers and hurts larger
models, and at 4B it cannot be both helpful and safe at once---of two same-size
candidates, the more helpful one commits genuine dangerous errors, so we deploy
the other, which is about twice as faithful to its sources (as faithful as a
frontier model), and recover its helpfulness with a redesigned prompt that cuts
deflection from 33\% to 3\%. Corpus quality is decisive for the same reason: the
small model leans on what it retrieves, so where the corpus holds the right
passage the answer is specific and actionable, and where it does not the answer
goes vague. On a current Android device the system returns a cited answer in tens of
seconds, fully offline; lower-end field hardware is slower. MAM-AI is a
thoroughly evaluated, open-source research prototype, not a fielded product; the
system, knowledge-base pipeline, benchmarks, and evaluation harness are released.
\end{abstract}

\keywords{retrieval-augmented generation, on-device inference, medical AI,
global health, maternal health, evaluation}

\maketitle
% The visible author line carries the CJK name as a graphic; set the PDF
% metadata author to the plain-ASCII romanisation explicitly.
\hypersetup{pdfauthor={Ren Yi}}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Body
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Top-down structure mirroring the renyi.ch/projects/mamai webpage:
% lead with the deployed system and its headline result, then decompose
% into components, then the knowledge base underneath it all.
\input{sections/01_introduction}
\input{sections/02_system_design}
\input{sections/03_guideline_corpus}
\input{sections/04_evaluation_methodology}
\input{sections/05_endtoend_results}
\input{sections/06_retrieval_results}
\input{sections/07_generator_results}
\input{sections/08_latency_results}
\input{sections/09_discussion}
\input{sections/10_limitations}
\input{sections/11_related_work}
\input{sections/12_conclusion}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Bibliography
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\bibliography{references}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Appendix
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\input{sections/appendix_prompts}
\input{sections/appendix_case_nausea}
\input{sections/appendix_contradictions}

\end{document}
